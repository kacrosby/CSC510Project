---
all:
  children:
    botservers:
      hosts:
        vcl_node:
          ansible_host: 152.7.180.50
          ansible_port: 22
          ansible_user: "{{ username }}"
          ansible_password: "{{ password }}"
          ansible_ssh_extra_args: "-o StrictHostKeyChecking=no"
